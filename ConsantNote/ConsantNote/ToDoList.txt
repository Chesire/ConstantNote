Make a settings file, this should store all the data I.E what the selected files are. Or could use serialization to just keep the ViewModels set on relaunch
Add serialization to load files back up when app starts.

Improve GUI.

Add some sort of queueing for when it cannot save, so can try it after a few seconds.

Add a mechanic to save, instead of just on losing focus.

Need a proper close button, instead of the minimal button.