Add some sort of queueing for when it cannot save, so can try it after a few seconds.

Settings:
	Add a setting to create startup shortcut.

Create a Wix installer so the application can be installed - 
	Installer should create a short cut in the startup menu - C:\Users\CurrentUser\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup : Win 8



Possibilities:
	Make it non minimizable
	Make it possible to hide text. If below a tag (such as <ignore/>)
